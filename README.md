This project is a simple embedded temperature monitoring system developed using Arduino Uno and simulated in Proteus Design Suite. The system uses two temperature sensors to monitor and compare temperature values. If the temperature from the first sensor becomes higher than the second sensor, an LED indicator turns ON; otherwise, the LED remains OFF. The temperature values are displayed on an LCD display for real-time monitoring.
